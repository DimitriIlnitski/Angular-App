import { ListFilterCourseNamePipe } from './list-filter-course-name.pipe';

describe('ListFilterCourseNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ListFilterCourseNamePipe();
    expect(pipe).toBeTruthy();
  });
});
